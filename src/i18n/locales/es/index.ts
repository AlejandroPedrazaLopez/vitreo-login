// Export all Spanish translations
import { auth } from './auth';
import { common } from './common';
import { twoFA } from './twoFA';

export const es = {
  auth,
  twoFA,
  common
};

export default es; 